@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["VerifyOtp"];
}

<div class="container-fluid login-page">
    <div class="row justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="col-11 col-sm-8 col-md-5 col-lg-4 col-xl-3">
            <div class="card border-0 shadow-lg rounded-4 overflow-hidden">
                <div class="card-header bg-light py-4 text-center border-0 border-bottom">
                    <h3 class="text-dark fw-bold mb-0" data-i18n="VerifyOtp">@Localizer["VerifyOtp"]</h3>
                    <p class="text-muted small mb-0 mt-1" data-i18n="CheckYourEmailForCode">
                        @Localizer["CheckYourEmailForCode"]</p>
                </div>
                <div class="card-body p-4 p-md-5 bg-white">
                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger border-0 rounded-3 mb-4 py-2 px-3 small" role="alert">
                            <i class="bi bi-x-circle-fill me-2"></i>
                            @ViewBag.ErrorMessage
                        </div>
                    }

                    <form asp-action="VerifyOtp" method="post" autocomplete="off">
                        <div class="mb-4">
                            <label for="otp" class="form-label text-muted small fw-bold"
                                data-i18n="VerificationCode">@Localizer["VerificationCode"]</label>
                            <input name="otp" id="otp" class="form-control shadow-none py-2 text-center ls-2"
                                placeholder="000000" required maxlength="6" autocomplete="one-time-code" />
                        </div>

                        <div class="d-grid mt-4">
                            <button type="submit"
                                class="btn btn-primary btn-lg rounded-3 py-2 fw-bold transition-all shadow-sm border-0"
                                style="background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%); font-size: 1rem;">
                                <span data-i18n="VerifyCode">@Localizer["VerifyCode"]</span>
                            </button>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>
</div>

<style>
    .login-page {
        background: linear-gradient(135deg, #fdfbfb 0%, #ebedee 100%);
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow-y: auto;
    }

    .ls-2 {
        letter-spacing: 0.5rem;
        font-weight: bold;
        font-size: 1.2rem;
    }

    .extra-small {
        font-size: 0.75rem;
    }

    .form-control {
        background-color: #f8fafc !important;
        color: #0f172a !important;
        /* Black/Dark Navy for light theme */
        border: 1px solid #e2e8f0 !important;
        transition: all 0.2s ease-in-out !important;
        caret-color: #0f172a !important;
    }

    /* Standard Autofill for light mode */
    .form-control:-webkit-autofill,
    .form-control:-webkit-autofill:hover,
    .form-control:-webkit-autofill:focus,
    .form-control:-webkit-autofill:active {
        -webkit-box-shadow: 0 0 0 30px #f8fafc inset !important;
        -webkit-text-fill-color: #0f172a !important;
    }

    .form-control::placeholder {
        color: #94a3b8 !important;
        opacity: 1 !important;
    }

    .form-control:hover {
        background-color: #ffffff !important;
        border-color: #cbd5e1 !important;
    }

    .form-control:focus,
    .form-control:active {
        background-color: #ffffff !important;
        border-color: #11998e !important;
        color: #0f172a !important;
        box-shadow: 0 0 0 3px rgba(17, 153, 142, 0.2) !important;
        outline: none !important;
    }
</style>
<style>
    /* Dark Mode Support */
    [data-theme='dark'] .login-page {
        background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
    }

    [data-theme='dark'] .card {
        background-color: #1e1e2d !important;
        color: #e2e8f0;
    }

    [data-theme='dark'] .card-header {
        background-color: #1a1a27 !important;
        border-bottom-color: #2d2d3f !important;
    }

    [data-theme='dark'] .card-header h3,
    [data-theme='dark'] .card-header .text-dark {
        color: #fff !important;
    }

    [data-theme='dark'] .card-body {
        background-color: #1e1e2d !important;
    }

    [data-theme='dark'] .text-muted {
        color: #94a3b8 !important;
    }

    [data-theme='dark'] .form-control {
        background-color: #1e293b !important;
        color: #ffffff !important;
        /* White text for dark theme */
        border-color: #334155 !important;
        caret-color: #ffffff !important;
    }

    [data-theme='dark'] .form-control:-webkit-autofill,
    [data-theme='dark'] .form-control:-webkit-autofill:hover,
    [data-theme='dark'] .form-control:-webkit-autofill:focus,
    [data-theme='dark'] .form-control:-webkit-autofill:active {
        -webkit-box-shadow: 0 0 0 30px #1e293b inset !important;
        -webkit-text-fill-color: #ffffff !important;
    }

    [data-theme='dark'] .form-control::placeholder {
        color: #cbd5e1 !important;
    }

    [data-theme='dark'] .form-control:hover {
        background-color: #243049 !important;
        border-color: #475569 !important;
    }

    [data-theme='dark'] .form-control:focus {
        background-color: #1e293b !important;
        border-color: #11998e !important;
        color: #ffffff !important;
    }

    [data-theme='dark'] .bg-light {
        background-color: #1a1a27 !important;
    }

    [data-theme='dark'] .bg-white {
        background-color: #1e1e2d !important;
    }
</style>

@section Scripts {
    <script>
        window.onload = function () {
            var input = document.getElementById('otp');
            if (input) input.value = '';
        };
    </script>
}
