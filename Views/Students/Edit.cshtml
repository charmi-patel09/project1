@model JsonCrudApp.Models.Student

@{
    ViewData["Title"] = "Edit User";
    ViewData["TitleKey"] = "EditUser";
}

@* Use Partial View for Form Logic *@
@await Html.PartialAsync("_UserForm", Model, new ViewDataDictionary(ViewData) { { "IsEdit", true } })

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
