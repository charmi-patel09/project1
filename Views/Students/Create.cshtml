@model JsonCrudApp.Models.Student

@{
    ViewData["Title"] = "Create New User";
    ViewData["TitleKey"] = "CreateUser";
}

@* Use Partial View for Form Logic *@
@await Html.PartialAsync("_UserForm", Model, new ViewDataDictionary(ViewData) { { "IsEdit", false } })

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
